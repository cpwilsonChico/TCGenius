import 'package:flutter/material.dart';
import 'package:tcgenius/scry.dart';
import 'package:tcgenius/main.dart';

class cardScroll extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Scroll"),
      ),
      body: Center(
      child: ListView(

      ),
      ),
    );

  }
}
